# Google Maps Platform API key with "Map Tiles API" enabled.
# Without it the game runs in OSM fallback mode (real streets, extruded buildings).
# With it you get photorealistic Google Earth 3D of Olivos.
GOOGLE_MAPS_API_KEY=
PORT=8080
